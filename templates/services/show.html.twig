{% extends 'base.html.twig' %}

{% block title %}Services{% endblock %}

{% block content %}
    <h1 class="text-2xl font-bold mb-4">Voir un service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ service.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service.description }}</td>
            </tr>
            <tr>
                <th>Images</th>
                <td>
                    {% for picture in service.picture %}
                        <img src="{{ asset('uploads/services/' ~ picture.file) }}" class="w-12 h-12 cover" alt="Service Picture">
                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_services_index') }}">back to list</a>

    <a href="{{ path('app_services_edit', {'id': service.id}) }}">edit</a>

    {{ include('services/_delete_form.html.twig') }}
{% endblock %}
